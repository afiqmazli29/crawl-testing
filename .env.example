# Firecrawl API key — get one at https://firecrawl.dev
FIRECRAWL_API_KEY=fc-your-key-here

# Database URL (SQLite by default; swap to postgres://... for PostgreSQL)
DATABASE_URL=sqlite:halal.db?mode=rwc
